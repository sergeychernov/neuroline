{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack yarn build:packages && corepack yarn workspace @neuroline/nextjs build",
  "installCommand": "corepack yarn install",
  "framework": "nextjs",
  "outputDirectory": "apps/nextjs/.next",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}