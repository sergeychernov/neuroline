{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "yarn build:packages && yarn workspace @neuroline/nextjs build",
  "installCommand": "corepack enable && corepack prepare yarn@4.12.0 --activate && yarn install",
  "framework": "nextjs",
  "outputDirectory": "apps/nextjs/.next",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}